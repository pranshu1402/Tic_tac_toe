import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// Learnings:
// Return doesnt work inside foreach Loops
// CSS elements positioning
// Function passing between components
// passing props 
// disabling elements based on state